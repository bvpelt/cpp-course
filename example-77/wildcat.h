#ifndef WILDCAT_H
#define WILDCAT_H

/*
#include "cat.h"

class Wildcat: public Cat {   // error: cannot derive from ‘final’ base ‘Cat’ in derived type ‘Wildcat’
public:
    Wildcat() = default;

    Wildcat(std::string &color, std::string &description) : Cat(color, description){};

    Wildcat(char const *color, char const *description) : Cat(color, description){};

    ~Wildcat() = default;
};
*/
#endif